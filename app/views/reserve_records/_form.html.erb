<%= form_with(model: reserve_record, local: true) do |form| %>
  <% if reserve_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reserve_record.errors.count, "error") %> prohibited this reserve_record from being saved:</h2>

      <ul>
      <% reserve_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
